server:
  port: 8072
 # we want gateway server to register with eureka so that the gateway server may know about other microservices
eureka:
  instance:
    preferIpAddress: true
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: "http://localhost:8070/eureka/"
